import requests

# show_list_url = 'http://127.0.0.1:5000/show-list'

# res = requests.put(show_list_url, {'show': 'Endeavour'})
# print(res.status_code)
# print(res.json())

# res = requests.get(show_list_url)
# print(res.json())

poi = {
    'title': 'Person of Interest',
    'series_num': '1',
    'episode_num': '1',
    'episode_title': 'Pilot',
    'channel': 'TEN'
}